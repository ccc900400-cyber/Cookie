技术文档与使用说明

1. 项目概述

Cookie 是一款集成式 AI 应用，旨在通过先进的语言模型简化复杂信息的处理流程。它将长文档、长视频、实时会议语音转化为结构化的知识报告，并提供即时的多模态问答支持。

2. 详细功能说明

2.1 千析模块 (Analysis Agent)
- 文件导入：支持 PDF、Word、视频等多种格式的本地上传。
- 链接解析：输入合法的视频 URL，系统将尝试提取视频内容并进行总结。
- 智能报告：AI 会根据预设的专业分析提示词，生成包含内容概览、核心要点、深度分析和结论建议的报告。
- 导出功能：利用 `docx` 库在前端生成 Word 文档，自动清理 Markdown 符号以保证排版整洁。

2.2 百闻模块 (QA Agent)
- 对话流：支持上下文关联的连续对话，采用流式响应提升交互体验。
- 建议系统：系统会定期更新 2027 年实时热点话题，引导用户进行探索。
- 消息管理：支持消息复制、清空历史记录等操作。

2.3 纪要模块 (Minutes Agent)
- 音频采集：利用 Web Audio API 实现高质量的实时录音。
- 智能转写：将音频流发送至 Gemini 模型，识别不同发言人并生成完整转录。
- 自动纪要：提取会议核心议题、观点、结论及待办事项，形成结构化文档。

3. 技术实现细节

3.1 AI 服务集成
应用使用 `@google/genai` SDK 与 `Gemini 3 Flash` 模型通信。针对大文件和长音频，采用流式传输技术确保前端界面的实时响应。

3.2 音频处理技术
- 采用 `MediaRecorder` API 进行音频采集。
- 利用 `AudioContext` 进行实时音量可视化监控。
- 音频数据通过 Base64 编码后传输至 AI 模型进行多模态理解。

3.3 数据存储方案
- IndexedDB：用于存储大量的聊天记录、分析历史和会议纪要，确保刷新页面后数据不丢失。
- LocalStorage：用于存储用户偏好设置（如语言、主题）以及短期的热点话题缓存。

3.4 响应式与国际化
- 响应式布局：针对移动端、平板和电脑端进行了差异化布局优化，特别是字体大小（Mobile: `prose-sm`, Desktop: `prose-base`）。
- 国际化 (i18n)：支持简体中文、繁体中文和英文，通过自定义 hook 实现即时切换。

4. 部署与维护

4.1 环境变量
- GEMINI_API_KEY：必需。用于身份验证。

4.2 构建生产版本
运行 `npm run build` 将生成优化后的静态资源到 `dist` 目录。

5. 常见问题排查

- 权限问题：使用“纪要”功能时，系统会请求麦克风权限，请务必授予。
- 响应超时：若遇到网络波动，系统内置了流式容错机制。
- 文件限制：单个文件建议不超过 100MB 以获得最佳解析效果。
- 格式清理：系统会自动移除 AI 输出中的 `**` 加粗符号，以确保在 Markdown 渲染器和 Word 导出中保持一致的视觉效果。
